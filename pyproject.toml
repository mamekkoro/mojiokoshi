[project]
name = "mojiokoshi"
version = "0.1.0"
description = "Transcription and speaker diarization workflow"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pyannote.audio",
    "huggingface-hub",
]
